(require 'eproject)
(require 'eproject-extras)

(define-project-type python (generic)
  (look-for "setup.python")
  :relevant-files ("\\.py"))

(define-project-type elisp (generic)
  (look-for "setup.lisp")
  :relevant-files ("\\.el"))


(provide 'rds-eproject)