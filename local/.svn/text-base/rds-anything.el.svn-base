;; rds-anything.el ---

(require 'anything)
(require 'anything-config)

;; The prefix key for all anything-command-map commands
;;(setq anything-command-map-prefix-key "<f2> a")


(provide 'rds-anything)