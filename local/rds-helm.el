;; rds-helm.el ---

;;; Code:
(require 'helm-config)
(setq helm-ff-auto-update-initial-value t)
(setq helm-M-x-requires-pattern nil)

(require 'powerline)


(provide 'rds-helm)
;;; rds-helm.el ends here
